// Chat is rendered as a persistent pane in the app layout (so toggling between
// Boards and Chat never unmounts either side). This route only anchors the URL
// — the layout shows the chat pane when the path starts with /chat.
export default function ChatPage() {
  return null;
}
