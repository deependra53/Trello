// Deep link to a channel. The layout reads the channelId from the URL and hands
// it to the persistent ChatApp; this route only anchors the URL.
export default function ChatChannelPage() {
  return null;
}
